export enum DialogType {
  success = 'success',
  error = 'error',
  warning = 'warning',
  custom = 'custom',
}
export enum TokenActionType {
  receive = 'receive',
  send = 'send',
  swap = 'swap',
}
